// Automatically generated from template - EDITS WILL BE LOST

// Generated by iodcomp.awk with options verify iodcompv

#ifndef __Header_iodcompv__
#define __Header_iodcompv__

bool
CompositeIOD_CRImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "CRImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (CRSeries && !CRSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying CRSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (CRImage && !CRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying CRImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (ModalityLUT && !ModalityLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying ModalityLUT " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CRImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_CTImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "CTImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePlane && !ImagePlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying ImagePlane " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (CTImage && !CTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying CTImage " << (success ? "success" : "failure") << endl;

	if (MultienergyCTImage && !MultienergyCTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying MultienergyCTImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CTImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MRImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MRImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePlane && !ImagePlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying ImagePlane " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (MRImage && !MRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying MRImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_NMImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "NMImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (NMPETPatientOrientation && !NMPETPatientOrientation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMPETPatientOrientation " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (NMImagePixel && !NMImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMImagePixel " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (NMMultiFrame && !NMMultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMMultiFrame " << (success ? "success" : "failure") << endl;

	if (NMImage && !NMImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMImage " << (success ? "success" : "failure") << endl;

	if (NMIsotope && !NMIsotope->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMIsotope " << (success ? "success" : "failure") << endl;

	if (NMDetector && !NMDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMDetector " << (success ? "success" : "failure") << endl;

	if (NMTomoAcquisition && !NMTomoAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMTomoAcquisition " << (success ? "success" : "failure") << endl;

	if (NMMultiGatedAcquisition && !NMMultiGatedAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMMultiGatedAcquisition " << (success ? "success" : "failure") << endl;

	if (NMPhase && !NMPhase->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMPhase " << (success ? "success" : "failure") << endl;

	if (NMReconstruction && !NMReconstruction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying NMReconstruction " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (MultiFrameOverlay && !MultiFrameOverlay->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying MultiFrameOverlay " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "NMImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_USImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "USImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (PaletteColorLookupTable && !PaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying PaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (USRegionCalibration && !USRegionCalibration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying USRegionCalibration " << (success ? "success" : "failure") << endl;

	if (USImage && !USImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying USImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_USMultiFrameImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "USMultiFrameImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (FramePointers && !FramePointers->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying FramePointers " << (success ? "success" : "failure") << endl;

	if (PaletteColorLookupTable && !PaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying PaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (USRegionCalibration && !USRegionCalibration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying USRegionCalibration " << (success ? "success" : "failure") << endl;

	if (USImage && !USImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying USImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (MultiFrameOverlay && !MultiFrameOverlay->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying MultiFrameOverlay " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "USMultiFrameImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_SCImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "SCImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SCEquipment && !SCEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying SCEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePlane && !ImagePlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying ImagePlane " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SCImage && !SCImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying SCImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (ModalityLUT && !ModalityLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying ModalityLUT " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SCImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MultiframeSingleBitSCImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MultiframeSingleBitSCImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SCEquipment && !SCEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying SCEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (FramePointers && !FramePointers->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying FramePointers " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SCImage && !SCImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying SCImage " << (success ? "success" : "failure") << endl;

	if (SCMultiFrameImage && !SCMultiFrameImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying SCMultiFrameImage " << (success ? "success" : "failure") << endl;

	if (SCMultiFrameVector && !SCMultiFrameVector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying SCMultiFrameVector " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	if (MultiframeSingleBitSCImagePseudo && !MultiframeSingleBitSCImagePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeSingleBitSCImage success after verifying MultiframeSingleBitSCImagePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MultiframeGrayscaleByteSCImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MultiframeGrayscaleByteSCImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SCEquipment && !SCEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying SCEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (FramePointers && !FramePointers->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying FramePointers " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForMFSC && !MultiFrameFunctionalGroupsForMFSC->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying MultiFrameFunctionalGroupsForMFSC " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SCImage && !SCImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying SCImage " << (success ? "success" : "failure") << endl;

	if (SCMultiFrameImage && !SCMultiFrameImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying SCMultiFrameImage " << (success ? "success" : "failure") << endl;

	if (SCMultiFrameVector && !SCMultiFrameVector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying SCMultiFrameVector " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	if (MultiframeGrayscaleByteSCImagePseudo && !MultiframeGrayscaleByteSCImagePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleByteSCImage success after verifying MultiframeGrayscaleByteSCImagePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MultiframeGrayscaleWordSCImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MultiframeGrayscaleWordSCImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SCEquipment && !SCEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying SCEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (FramePointers && !FramePointers->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying FramePointers " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForMFSC && !MultiFrameFunctionalGroupsForMFSC->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying MultiFrameFunctionalGroupsForMFSC " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SCImage && !SCImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying SCImage " << (success ? "success" : "failure") << endl;

	if (SCMultiFrameImage && !SCMultiFrameImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying SCMultiFrameImage " << (success ? "success" : "failure") << endl;

	if (SCMultiFrameVector && !SCMultiFrameVector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying SCMultiFrameVector " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	if (MultiframeGrayscaleWordSCImagePseudo && !MultiframeGrayscaleWordSCImagePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeGrayscaleWordSCImage success after verifying MultiframeGrayscaleWordSCImagePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MultiframeTrueColorSCImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MultiframeTrueColorSCImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SCEquipment && !SCEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying SCEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (FramePointers && !FramePointers->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying FramePointers " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForMFSC && !MultiFrameFunctionalGroupsForMFSC->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying MultiFrameFunctionalGroupsForMFSC " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SCImage && !SCImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying SCImage " << (success ? "success" : "failure") << endl;

	if (SCMultiFrameImage && !SCMultiFrameImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying SCMultiFrameImage " << (success ? "success" : "failure") << endl;

	if (SCMultiFrameVector && !SCMultiFrameVector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying SCMultiFrameVector " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	if (MultiframeTrueColorSCImagePseudo && !MultiframeTrueColorSCImagePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MultiframeTrueColorSCImage success after verifying MultiframeTrueColorSCImagePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_StandaloneOverlay::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "StandaloneOverlay";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (OverlayIdentification && !OverlayIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying OverlayIdentification " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneOverlay success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_StandaloneCurve::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "StandaloneCurve";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (CurveIdentification && !CurveIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying CurveIdentification " << (success ? "success" : "failure") << endl;

	if (Curve && !Curve->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying Curve " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneCurve success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_StandaloneModalityLUT::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "StandaloneModalityLUT";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ModalityLUT && !ModalityLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying ModalityLUT " << (success ? "success" : "failure") << endl;

	if (LUTIdentification && !LUTIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying LUTIdentification " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneModalityLUT success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_StandaloneVOILUT::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "StandaloneVOILUT";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (LUTIdentification && !LUTIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying LUTIdentification " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StandaloneVOILUT success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_Segmentation::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "Segmentation";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (SegmentationSeries && !SegmentationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying SegmentationSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (MultiResolutionPyramid && !MultiResolutionPyramid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying MultiResolutionPyramid " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (MicroscopeSlideLayerTileOrganization && !MicroscopeSlideLayerTileOrganization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying MicroscopeSlideLayerTileOrganization " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (SegmentationImage && !SegmentationImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying SegmentationImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForSegmentation && !MultiFrameFunctionalGroupsForSegmentation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying MultiFrameFunctionalGroupsForSegmentation " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (PaletteColorLookupTable && !PaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying PaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Segmentation success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_SurfaceSegmentation::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "SurfaceSegmentation";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (SegmentationSeries && !SegmentationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying SegmentationSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SurfaceSegmentation && !SurfaceSegmentation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying SurfaceSegmentation " << (success ? "success" : "failure") << endl;

	if (SurfaceMesh && !SurfaceMesh->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying SurfaceMesh " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SurfaceSegmentation success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_SpatialRegistration::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "SpatialRegistration";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (SpatialRegistrationSeries && !SpatialRegistrationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying SpatialRegistrationSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SpatialRegistration && !SpatialRegistration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying SpatialRegistration " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialRegistration success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_DeformableSpatialRegistration::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "DeformableSpatialRegistration";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (SpatialRegistrationSeries && !SpatialRegistrationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying SpatialRegistrationSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (DeformableSpatialRegistration && !DeformableSpatialRegistration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying DeformableSpatialRegistration " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DeformableSpatialRegistration success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_SpatialFiducials::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "SpatialFiducials";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (SpatialFiducialsSeries && !SpatialFiducialsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying SpatialFiducialsSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SpatialFiducials && !SpatialFiducials->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying SpatialFiducials " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpatialFiducials success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EncapsulatedPDF::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EncapsulatedPDF";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocumentSeries && !EncapsulatedDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying EncapsulatedDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SCEquipment && !SCEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying SCEquipment " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocument && !EncapsulatedDocument->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying EncapsulatedDocument " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocumentPDFPseudo && !EncapsulatedDocumentPDFPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying EncapsulatedDocumentPDFPseudo " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedPDF success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EncapsulatedCDA::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EncapsulatedCDA";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocumentSeries && !EncapsulatedDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying EncapsulatedDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SCEquipment && !SCEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying SCEquipment " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocument && !EncapsulatedDocument->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying EncapsulatedDocument " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocumentCDAPseudo && !EncapsulatedDocumentCDAPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying EncapsulatedDocumentCDAPseudo " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedCDA success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EncapsulatedSTL::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EncapsulatedSTL";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocumentSeries && !EncapsulatedDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying EncapsulatedDocumentSeries " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocumentSTLSeriesPseudo && !EncapsulatedDocumentSTLSeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying EncapsulatedDocumentSTLSeriesPseudo " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocument && !EncapsulatedDocument->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying EncapsulatedDocument " << (success ? "success" : "failure") << endl;

	if (EncapsulatedDocumentSTLPseudo && !EncapsulatedDocumentSTLPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying EncapsulatedDocumentSTLPseudo " << (success ? "success" : "failure") << endl;

	if (Manufacturing3DModel && !Manufacturing3DModel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying Manufacturing3DModel " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EncapsulatedSTL success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RealWorldValueMapping::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RealWorldValueMapping";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (RealWorldValueMappingSeries && !RealWorldValueMappingSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying RealWorldValueMappingSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (RealWorldValueMapping && !RealWorldValueMapping->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying RealWorldValueMapping " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RealWorldValueMapping success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_IVOCTImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "IVOCTImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (IntravascularOCTSeries && !IntravascularOCTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying IntravascularOCTSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (SupplementalPaletteColorLUT && !SupplementalPaletteColorLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying SupplementalPaletteColorLUT " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForIVOCTImageForPresentation && !MultiFrameFunctionalGroupsForIVOCTImageForPresentation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying MultiFrameFunctionalGroupsForIVOCTImageForPresentation " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForIVOCTImageForProcessing && !MultiFrameFunctionalGroupsForIVOCTImageForProcessing->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying MultiFrameFunctionalGroupsForIVOCTImageForProcessing " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (IntravascularOCTImage && !IntravascularOCTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying IntravascularOCTImage " << (success ? "success" : "failure") << endl;

	if (IntravascularOCTAcquisitionParameters && !IntravascularOCTAcquisitionParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying IntravascularOCTAcquisitionParameters " << (success ? "success" : "failure") << endl;

	if (IntravascularOCTProcessingParameters && !IntravascularOCTProcessingParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying IntravascularOCTProcessingParameters " << (success ? "success" : "failure") << endl;

	if (IntravascularImageAcquisitionParameters && !IntravascularImageAcquisitionParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying IntravascularImageAcquisitionParameters " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IVOCTImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_ParametricMap::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "ParametricMap";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ParametricMapSeries && !ParametricMapSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying ParametricMapSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (MultiResolutionPyramid && !MultiResolutionPyramid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying MultiResolutionPyramid " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (MicroscopeSlideLayerTileOrganization && !MicroscopeSlideLayerTileOrganization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying MicroscopeSlideLayerTileOrganization " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (FloatingPointImagePixel && !FloatingPointImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying FloatingPointImagePixel " << (success ? "success" : "failure") << endl;

	if (DoubleFloatingPointImagePixel && !DoubleFloatingPointImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying DoubleFloatingPointImagePixel " << (success ? "success" : "failure") << endl;

	if (ParametricMapImage && !ParametricMapImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying ParametricMapImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForParametricMap && !MultiFrameFunctionalGroupsForParametricMap->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying MultiFrameFunctionalGroupsForParametricMap " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (PaletteColorLookupTable && !PaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying PaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (BulkMotionSynchronization && !BulkMotionSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying BulkMotionSynchronization " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ParametricMap success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_BasicDirectory::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "BasicDirectory";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicDirectory success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (FileSetIdentification && !FileSetIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicDirectory success after verifying FileSetIdentification " << (success ? "success" : "failure") << endl;

	if (DirectoryInformation && !DirectoryInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicDirectory success after verifying DirectoryInformation " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_BasicDirectoryDental::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "BasicDirectoryDental";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicDirectoryDental success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (FileSetIdentification && !FileSetIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicDirectoryDental success after verifying FileSetIdentification " << (success ? "success" : "failure") << endl;

	if (DirectoryInformation && !DirectoryInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicDirectoryDental success after verifying DirectoryInformation " << (success ? "success" : "failure") << endl;

	if (DirectoryInformationDental && !DirectoryInformationDental->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicDirectoryDental success after verifying DirectoryInformationDental " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_XAImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "XAImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (FramePointers && !FramePointers->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying FramePointers " << (success ? "success" : "failure") << endl;

	if (Mask && !Mask->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying Mask " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (XRayImage && !XRayImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying XRayImage " << (success ? "success" : "failure") << endl;

	if (XRayAcquisition && !XRayAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying XRayAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (XRayTable && !XRayTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying XRayTable " << (success ? "success" : "failure") << endl;

	if (XAPositioner && !XAPositioner->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying XAPositioner " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (MultiFrameOverlay && !MultiFrameOverlay->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying MultiFrameOverlay " << (success ? "success" : "failure") << endl;

	if (ModalityLUT && !ModalityLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying ModalityLUT " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XAImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_XRFImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "XRFImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (FramePointers && !FramePointers->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying FramePointers " << (success ? "success" : "failure") << endl;

	if (Mask && !Mask->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying Mask " << (success ? "success" : "failure") << endl;

	if (XRayImage && !XRayImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying XRayImage " << (success ? "success" : "failure") << endl;

	if (XRayAcquisition && !XRayAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying XRayAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (XRayTable && !XRayTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying XRayTable " << (success ? "success" : "failure") << endl;

	if (XRFPositioner && !XRFPositioner->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying XRFPositioner " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (MultiFrameOverlay && !MultiFrameOverlay->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying MultiFrameOverlay " << (success ? "success" : "failure") << endl;

	if (ModalityLUT && !ModalityLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying ModalityLUT " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRFImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EnhancedXAImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EnhancedXAImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (XAXRFSeries && !XAXRFSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying XAXRFSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (Mask && !Mask->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying Mask " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForEnhancedXAImage && !MultiFrameFunctionalGroupsForEnhancedXAImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying MultiFrameFunctionalGroupsForEnhancedXAImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (EnhancedXAXRFImage && !EnhancedXAXRFImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying EnhancedXAXRFImage " << (success ? "success" : "failure") << endl;

	if (XAXRFAcquisition && !XAXRFAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying XAXRFAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayImageIntensifier && !XRayImageIntensifier->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying XRayImageIntensifier " << (success ? "success" : "failure") << endl;

	if (XRayDetector && !XRayDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying XRayDetector " << (success ? "success" : "failure") << endl;

	if (XAXRFMultiFramePresentation && !XAXRFMultiFramePresentation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying XAXRFMultiFramePresentation " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXAImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EnhancedXRFImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EnhancedXRFImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (XAXRFSeries && !XAXRFSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying XAXRFSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (Mask && !Mask->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying Mask " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForEnhancedXRFImage && !MultiFrameFunctionalGroupsForEnhancedXRFImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying MultiFrameFunctionalGroupsForEnhancedXRFImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (EnhancedXAXRFImage && !EnhancedXAXRFImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying EnhancedXAXRFImage " << (success ? "success" : "failure") << endl;

	if (XAXRFAcquisition && !XAXRFAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying XAXRFAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayImageIntensifier && !XRayImageIntensifier->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying XRayImageIntensifier " << (success ? "success" : "failure") << endl;

	if (XRayDetector && !XRayDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying XRayDetector " << (success ? "success" : "failure") << endl;

	if (XAXRFMultiFramePresentation && !XAXRFMultiFramePresentation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying XAXRFMultiFramePresentation " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedXRFImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_XRay3DAngiographicImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "XRay3DAngiographicImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedSeries && !EnhancedSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying EnhancedSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForXRay3DAngiographicImage && !MultiFrameFunctionalGroupsForXRay3DAngiographicImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying MultiFrameFunctionalGroupsForXRay3DAngiographicImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (PatientOrientation && !PatientOrientation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying PatientOrientation " << (success ? "success" : "failure") << endl;

	if (ImageEquipmentCoordinateRelationship && !ImageEquipmentCoordinateRelationship->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying ImageEquipmentCoordinateRelationship " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (XRay3DImage && !XRay3DImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying XRay3DImage " << (success ? "success" : "failure") << endl;

	if (XRay3DAngiographicImageContributingSources && !XRay3DAngiographicImageContributingSources->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying XRay3DAngiographicImageContributingSources " << (success ? "success" : "failure") << endl;

	if (XRay3DAngiographicAcquisition && !XRay3DAngiographicAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying XRay3DAngiographicAcquisition " << (success ? "success" : "failure") << endl;

	if (XRay3DReconstruction && !XRay3DReconstruction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying XRay3DReconstruction " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DAngiographicImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_XRay3DCraniofacialImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "XRay3DCraniofacialImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedSeries && !EnhancedSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying EnhancedSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForXRay3DCraniofacialImage && !MultiFrameFunctionalGroupsForXRay3DCraniofacialImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying MultiFrameFunctionalGroupsForXRay3DCraniofacialImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (PatientOrientation && !PatientOrientation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying PatientOrientation " << (success ? "success" : "failure") << endl;

	if (ImageEquipmentCoordinateRelationship && !ImageEquipmentCoordinateRelationship->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying ImageEquipmentCoordinateRelationship " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (XRay3DImage && !XRay3DImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying XRay3DImage " << (success ? "success" : "failure") << endl;

	if (XRay3DCraniofacialImageContributingSources && !XRay3DCraniofacialImageContributingSources->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying XRay3DCraniofacialImageContributingSources " << (success ? "success" : "failure") << endl;

	if (XRay3DCraniofacialAcquisition && !XRay3DCraniofacialAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying XRay3DCraniofacialAcquisition " << (success ? "success" : "failure") << endl;

	if (XRay3DReconstruction && !XRay3DReconstruction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying XRay3DReconstruction " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRay3DCraniofacialImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_PETImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "PETImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (PETSeries && !PETSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying PETSeries " << (success ? "success" : "failure") << endl;

	if (PETIsotope && !PETIsotope->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying PETIsotope " << (success ? "success" : "failure") << endl;

	if (PETMultigatedAcquisition && !PETMultigatedAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying PETMultigatedAcquisition " << (success ? "success" : "failure") << endl;

	if (NMPETPatientOrientation && !NMPETPatientOrientation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying NMPETPatientOrientation " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePlane && !ImagePlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying ImagePlane " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (PETImage && !PETImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying PETImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PETImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EnhancedPETImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EnhancedPETImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedPETSeries && !EnhancedPETSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying EnhancedPETSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForEnhancedPETImage && !MultiFrameFunctionalGroupsForEnhancedPETImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying MultiFrameFunctionalGroupsForEnhancedPETImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedPETIsotope && !EnhancedPETIsotope->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying EnhancedPETIsotope " << (success ? "success" : "failure") << endl;

	if (EnhancedPETAcquisition && !EnhancedPETAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying EnhancedPETAcquisition " << (success ? "success" : "failure") << endl;

	if (EnhancedPETImage && !EnhancedPETImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying EnhancedPETImage " << (success ? "success" : "failure") << endl;

	if (EnhancedPETCorrections && !EnhancedPETCorrections->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying EnhancedPETCorrections " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedPETImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_LegacyConvertedEnhancedPETImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "LegacyConvertedEnhancedPETImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedPETSeries && !EnhancedPETSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying EnhancedPETSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForLegacyConvertedEnhancedPETImage && !MultiFrameFunctionalGroupsForLegacyConvertedEnhancedPETImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying MultiFrameFunctionalGroupsForLegacyConvertedEnhancedPETImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedPETImage && !EnhancedPETImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying EnhancedPETImage " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedPETImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_PrivatePixelMedLegacyConvertedEnhancedPETImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "PrivatePixelMedLegacyConvertedEnhancedPETImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedPETSeries && !EnhancedPETSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying EnhancedPETSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForPrivatePixelMedLegacyConvertedEnhancedPETImage && !MultiFrameFunctionalGroupsForPrivatePixelMedLegacyConvertedEnhancedPETImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying MultiFrameFunctionalGroupsForPrivatePixelMedLegacyConvertedEnhancedPETImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedPETImage && !EnhancedPETImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying EnhancedPETImage " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedPETImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RTImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RTImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (RTSeries && !RTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying RTSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (RTImage && !RTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying RTImage " << (success ? "success" : "failure") << endl;

	if (ModalityLUT && !ModalityLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying ModalityLUT " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (Approval && !Approval->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying Approval " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RTDose::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RTDose";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (RTSeries && !RTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying RTSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePlane && !ImagePlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying ImagePlane " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (MultiFrameOverlay && !MultiFrameOverlay->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying MultiFrameOverlay " << (success ? "success" : "failure") << endl;

	if (ModalityLUT && !ModalityLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying ModalityLUT " << (success ? "success" : "failure") << endl;

	if (RTDose && !RTDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying RTDose " << (success ? "success" : "failure") << endl;

	if (RTDVH && !RTDVH->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying RTDVH " << (success ? "success" : "failure") << endl;

	if (StructureSet && !StructureSet->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying StructureSet " << (success ? "success" : "failure") << endl;

	if (ROIContour && !ROIContour->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying ROIContour " << (success ? "success" : "failure") << endl;

	if (RTDoseROI && !RTDoseROI->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying RTDoseROI " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTDose success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RTStructureSet::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RTStructureSet";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (RTSeries && !RTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying RTSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (StructureSet && !StructureSet->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying StructureSet " << (success ? "success" : "failure") << endl;

	if (ROIContour && !ROIContour->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying ROIContour " << (success ? "success" : "failure") << endl;

	if (RTROIObservations && !RTROIObservations->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying RTROIObservations " << (success ? "success" : "failure") << endl;

	if (Approval && !Approval->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying Approval " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTStructureSet success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RTPlan::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RTPlan";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (RTSeries && !RTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying RTSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (RTGeneralPlan && !RTGeneralPlan->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying RTGeneralPlan " << (success ? "success" : "failure") << endl;

	if (RTPrescription && !RTPrescription->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying RTPrescription " << (success ? "success" : "failure") << endl;

	if (RTToleranceTables && !RTToleranceTables->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying RTToleranceTables " << (success ? "success" : "failure") << endl;

	if (RTPatientSetup && !RTPatientSetup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying RTPatientSetup " << (success ? "success" : "failure") << endl;

	if (RTFractionScheme && !RTFractionScheme->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying RTFractionScheme " << (success ? "success" : "failure") << endl;

	if (RTBeams && !RTBeams->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying RTBeams " << (success ? "success" : "failure") << endl;

	if (RTBrachyApplicationSetups && !RTBrachyApplicationSetups->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying RTBrachyApplicationSetups " << (success ? "success" : "failure") << endl;

	if (Approval && !Approval->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying Approval " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTPlan success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RTBeamsTreatmentRecord::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RTBeamsTreatmentRecord";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (RTSeries && !RTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying RTSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (RTGeneralTreatmentRecord && !RTGeneralTreatmentRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying RTGeneralTreatmentRecord " << (success ? "success" : "failure") << endl;

	if (RTPatientSetup && !RTPatientSetup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying RTPatientSetup " << (success ? "success" : "failure") << endl;

	if (RTTreatmentMachineRecord && !RTTreatmentMachineRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying RTTreatmentMachineRecord " << (success ? "success" : "failure") << endl;

	if (MeasuredDoseReferenceRecord && !MeasuredDoseReferenceRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying MeasuredDoseReferenceRecord " << (success ? "success" : "failure") << endl;

	if (CalculatedDoseReferenceRecord && !CalculatedDoseReferenceRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying CalculatedDoseReferenceRecord " << (success ? "success" : "failure") << endl;

	if (RTBeamsSessionRecord && !RTBeamsSessionRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying RTBeamsSessionRecord " << (success ? "success" : "failure") << endl;

	if (RTTreatmentSummaryRecord && !RTTreatmentSummaryRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying RTTreatmentSummaryRecord " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBeamsTreatmentRecord success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RTBrachyTreatmentRecord::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RTBrachyTreatmentRecord";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (RTSeries && !RTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying RTSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (RTGeneralTreatmentRecord && !RTGeneralTreatmentRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying RTGeneralTreatmentRecord " << (success ? "success" : "failure") << endl;

	if (RTPatientSetup && !RTPatientSetup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying RTPatientSetup " << (success ? "success" : "failure") << endl;

	if (RTTreatmentMachineRecord && !RTTreatmentMachineRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying RTTreatmentMachineRecord " << (success ? "success" : "failure") << endl;

	if (MeasuredDoseReferenceRecord && !MeasuredDoseReferenceRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying MeasuredDoseReferenceRecord " << (success ? "success" : "failure") << endl;

	if (CalculatedDoseReferenceRecord && !CalculatedDoseReferenceRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying CalculatedDoseReferenceRecord " << (success ? "success" : "failure") << endl;

	if (RTBrachySessionRecord && !RTBrachySessionRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying RTBrachySessionRecord " << (success ? "success" : "failure") << endl;

	if (RTTreatmentSummaryRecord && !RTTreatmentSummaryRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying RTTreatmentSummaryRecord " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTBrachyTreatmentRecord success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RTTreatmentSummaryRecord::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RTTreatmentSummaryRecord";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (RTSeries && !RTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying RTSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (RTGeneralTreatmentRecord && !RTGeneralTreatmentRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying RTGeneralTreatmentRecord " << (success ? "success" : "failure") << endl;

	if (RTTreatmentSummaryRecord && !RTTreatmentSummaryRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying RTTreatmentSummaryRecord " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTTreatmentSummaryRecord success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RTIonPlan::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RTIonPlan";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (RTSeries && !RTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying RTSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (RTGeneralPlan && !RTGeneralPlan->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying RTGeneralPlan " << (success ? "success" : "failure") << endl;

	if (RTPrescription && !RTPrescription->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying RTPrescription " << (success ? "success" : "failure") << endl;

	if (RTIonToleranceTables && !RTIonToleranceTables->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying RTIonToleranceTables " << (success ? "success" : "failure") << endl;

	if (RTPatientSetup && !RTPatientSetup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying RTPatientSetup " << (success ? "success" : "failure") << endl;

	if (RTFractionScheme && !RTFractionScheme->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying RTFractionScheme " << (success ? "success" : "failure") << endl;

	if (RTIonBeams && !RTIonBeams->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying RTIonBeams " << (success ? "success" : "failure") << endl;

	if (Approval && !Approval->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying Approval " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonPlan success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RTIonBeamsTreatmentRecord::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RTIonBeamsTreatmentRecord";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (RTSeries && !RTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying RTSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (RTGeneralTreatmentRecord && !RTGeneralTreatmentRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying RTGeneralTreatmentRecord " << (success ? "success" : "failure") << endl;

	if (RTPatientSetup && !RTPatientSetup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying RTPatientSetup " << (success ? "success" : "failure") << endl;

	if (RTTreatmentMachineRecord && !RTTreatmentMachineRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying RTTreatmentMachineRecord " << (success ? "success" : "failure") << endl;

	if (MeasuredDoseReferenceRecord && !MeasuredDoseReferenceRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying MeasuredDoseReferenceRecord " << (success ? "success" : "failure") << endl;

	if (CalculatedDoseReferenceRecord && !CalculatedDoseReferenceRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying CalculatedDoseReferenceRecord " << (success ? "success" : "failure") << endl;

	if (RTIonBeamsSessionRecord && !RTIonBeamsSessionRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying RTIonBeamsSessionRecord " << (success ? "success" : "failure") << endl;

	if (RTTreatmentSummaryRecord && !RTTreatmentSummaryRecord->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying RTTreatmentSummaryRecord " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RTIonBeamsTreatmentRecord success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_DXImageForProcessing::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "DXImageForProcessing";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForProcessing success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_DXImageForPresentation::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "DXImageForPresentation";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentation success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MammographyImageForProcessing::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MammographyImageForProcessing";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (MammographySeries && !MammographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying MammographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (MammographyImage && !MammographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying MammographyImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessing success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MammographyImageForPresentation::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MammographyImageForPresentation";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (MammographySeries && !MammographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying MammographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (MammographyImage && !MammographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying MammographyImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentation success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MammographyImageForProcessingIHEMammo::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MammographyImageForProcessingIHEMammo";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (MammographySeries && !MammographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying MammographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (MammographyImage && !MammographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying MammographyImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfile && !IHEMammoProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying IHEMammoProfile " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfileWithoutPartialViewOption && !IHEMammoProfileWithoutPartialViewOption->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammo success after verifying IHEMammoProfileWithoutPartialViewOption " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MammographyImageForProcessingIHEMammoPartialViewOption::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MammographyImageForProcessingIHEMammoPartialViewOption";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (MammographySeries && !MammographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying MammographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (MammographyImage && !MammographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying MammographyImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfile && !IHEMammoProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying IHEMammoProfile " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfileWithPartialViewOption && !IHEMammoProfileWithPartialViewOption->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForProcessingIHEMammoPartialViewOption success after verifying IHEMammoProfileWithPartialViewOption " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MammographyImageForPresentationIHEMammo::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MammographyImageForPresentationIHEMammo";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (MammographySeries && !MammographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying MammographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (MammographyImage && !MammographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying MammographyImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfile && !IHEMammoProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying IHEMammoProfile " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfileWithoutPartialViewOption && !IHEMammoProfileWithoutPartialViewOption->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying IHEMammoProfileWithoutPartialViewOption " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfileForPresentationOnly && !IHEMammoProfileForPresentationOnly->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammo success after verifying IHEMammoProfileForPresentationOnly " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MammographyImageForPresentationIHEMammoPartialViewOption::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MammographyImageForPresentationIHEMammoPartialViewOption";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (MammographySeries && !MammographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying MammographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (MammographyImage && !MammographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying MammographyImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfile && !IHEMammoProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying IHEMammoProfile " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfileWithPartialViewOption && !IHEMammoProfileWithPartialViewOption->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying IHEMammoProfileWithPartialViewOption " << (success ? "success" : "failure") << endl;

	if (IHEMammoProfileForPresentationOnly && !IHEMammoProfileForPresentationOnly->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyImageForPresentationIHEMammoPartialViewOption success after verifying IHEMammoProfileForPresentationOnly " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_IntraoralImageForProcessing::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "IntraoralImageForProcessing";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (IntraoralSeries && !IntraoralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying IntraoralSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (IntraoralImage && !IntraoralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying IntraoralImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForProcessing success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_IntraoralImageForPresentation::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "IntraoralImageForPresentation";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (IntraoralSeries && !IntraoralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying IntraoralSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (IntraoralImage && !IntraoralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying IntraoralImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentation success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_IntraoralImageForPresentationDentalMedia::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "IntraoralImageForPresentationDentalMedia";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (IntraoralSeries && !IntraoralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying IntraoralSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (IntraoralImage && !IntraoralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying IntraoralImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (DentalImageOnMediaProfile && !DentalImageOnMediaProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraoralImageForPresentationDentalMedia success after verifying DentalImageOnMediaProfile " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_DXImageForPresentationDentalMedia::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "DXImageForPresentationDentalMedia";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (DXAnatomyImaged && !DXAnatomyImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying DXAnatomyImaged " << (success ? "success" : "failure") << endl;

	if (DXImage && !DXImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying DXImage " << (success ? "success" : "failure") << endl;

	if (DXDetector && !DXDetector->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying DXDetector " << (success ? "success" : "failure") << endl;

	if (XRayCollimator && !XRayCollimator->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying XRayCollimator " << (success ? "success" : "failure") << endl;

	if (DXPositioning && !DXPositioning->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying DXPositioning " << (success ? "success" : "failure") << endl;

	if (XRayTomographyAcquisition && !XRayTomographyAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying XRayTomographyAcquisition " << (success ? "success" : "failure") << endl;

	if (XRayAcquisitionDose && !XRayAcquisitionDose->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying XRayAcquisitionDose " << (success ? "success" : "failure") << endl;

	if (XRayGeneration && !XRayGeneration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying XRayGeneration " << (success ? "success" : "failure") << endl;

	if (XRayFiltration && !XRayFiltration->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying XRayFiltration " << (success ? "success" : "failure") << endl;

	if (XRayGrid && !XRayGrid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying XRayGrid " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (VOILUT && !VOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying VOILUT " << (success ? "success" : "failure") << endl;

	if (ImageHistogram && !ImageHistogram->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying ImageHistogram " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (DentalImageOnMediaProfile && !DentalImageOnMediaProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DXImageForPresentationDentalMedia success after verifying DentalImageOnMediaProfile " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_BreastTomosynthesisImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "BreastTomosynthesisImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedMammographySeries && !EnhancedMammographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying EnhancedMammographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForBreastTomosynthesisImage && !MultiFrameFunctionalGroupsForBreastTomosynthesisImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying MultiFrameFunctionalGroupsForBreastTomosynthesisImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (ImageEquipmentCoordinateRelationship && !ImageEquipmentCoordinateRelationship->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying ImageEquipmentCoordinateRelationship " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (XRay3DImage && !XRay3DImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying XRay3DImage " << (success ? "success" : "failure") << endl;

	if (BreastTomosynthesisContributingSources && !BreastTomosynthesisContributingSources->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying BreastTomosynthesisContributingSources " << (success ? "success" : "failure") << endl;

	if (BreastTomosynthesisAcquisition && !BreastTomosynthesisAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying BreastTomosynthesisAcquisition " << (success ? "success" : "failure") << endl;

	if (XRay3DReconstruction && !XRay3DReconstruction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying XRay3DReconstruction " << (success ? "success" : "failure") << endl;

	if (BreastView && !BreastView->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying BreastView " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_BreastTomosynthesisImageIHEDBT::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "BreastTomosynthesisImageIHEDBT";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedMammographySeries && !EnhancedMammographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying EnhancedMammographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForBreastTomosynthesisImage && !MultiFrameFunctionalGroupsForBreastTomosynthesisImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying MultiFrameFunctionalGroupsForBreastTomosynthesisImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (ImageEquipmentCoordinateRelationship && !ImageEquipmentCoordinateRelationship->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying ImageEquipmentCoordinateRelationship " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (XRay3DImage && !XRay3DImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying XRay3DImage " << (success ? "success" : "failure") << endl;

	if (BreastTomosynthesisContributingSources && !BreastTomosynthesisContributingSources->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying BreastTomosynthesisContributingSources " << (success ? "success" : "failure") << endl;

	if (BreastTomosynthesisAcquisition && !BreastTomosynthesisAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying BreastTomosynthesisAcquisition " << (success ? "success" : "failure") << endl;

	if (XRay3DReconstruction && !XRay3DReconstruction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying XRay3DReconstruction " << (success ? "success" : "failure") << endl;

	if (BreastView && !BreastView->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying BreastView " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	if (IHEDBTProfile && !IHEDBTProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastTomosynthesisImageIHEDBT success after verifying IHEDBTProfile " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_BreastProjectionXRayImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "BreastProjectionXRayImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (DXSeries && !DXSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying DXSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedMammographySeries && !EnhancedMammographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying EnhancedMammographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedMammographyImage && !EnhancedMammographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying EnhancedMammographyImage " << (success ? "success" : "failure") << endl;

	if (BreastView && !BreastView->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying BreastView " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Intervention && !Intervention->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying Intervention " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForBreastProjectionXRayImage && !MultiFrameFunctionalGroupsForBreastProjectionXRayImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying MultiFrameFunctionalGroupsForBreastProjectionXRayImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (PatientOrientation && !PatientOrientation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying PatientOrientation " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BreastProjectionXRayImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VLEndoscopicImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VLEndoscopicImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (VLEndoscopicSeriesPseudo && !VLEndoscopicSeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying VLEndoscopicSeriesPseudo " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (VLImage && !VLImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying VLImage " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLEndoscopicImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VLMicroscopicImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VLMicroscopicImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (VLMicroscopicSeriesPseudo && !VLMicroscopicSeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying VLMicroscopicSeriesPseudo " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (VLImage && !VLImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying VLImage " << (success ? "success" : "failure") << endl;

	if (OpticalPath && !OpticalPath->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying OpticalPath " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLMicroscopicImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VLSlideCoordinatesMicroscopicImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VLSlideCoordinatesMicroscopicImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (VLSlideCoordinatesMicroscopicSeriesPseudo && !VLSlideCoordinatesMicroscopicSeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying VLSlideCoordinatesMicroscopicSeriesPseudo " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (VLImage && !VLImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying VLImage " << (success ? "success" : "failure") << endl;

	if (SlideCoordinates && !SlideCoordinates->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying SlideCoordinates " << (success ? "success" : "failure") << endl;

	if (OpticalPath && !OpticalPath->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying OpticalPath " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLSlideCoordinatesMicroscopicImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VLPhotographicImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VLPhotographicImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (VLPhotographicSeriesPseudo && !VLPhotographicSeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying VLPhotographicSeriesPseudo " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (VLPhotographicEquipment && !VLPhotographicEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying VLPhotographicEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (VLImage && !VLImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying VLImage " << (success ? "success" : "failure") << endl;

	if (VLPhotographicAcquisition && !VLPhotographicAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying VLPhotographicAcquisition " << (success ? "success" : "failure") << endl;

	if (VLPhotographicGeolocation && !VLPhotographicGeolocation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying VLPhotographicGeolocation " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLPhotographicImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VideoEndoscopicImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VideoEndoscopicImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (VLEndoscopicSeriesPseudo && !VLEndoscopicSeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying VLEndoscopicSeriesPseudo " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (VLImage && !VLImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying VLImage " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoEndoscopicImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VideoMicroscopicImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VideoMicroscopicImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (VLMicroscopicSeriesPseudo && !VLMicroscopicSeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying VLMicroscopicSeriesPseudo " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (VLImage && !VLImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying VLImage " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoMicroscopicImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VideoPhotographicImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VideoPhotographicImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (VLPhotographicSeriesPseudo && !VLPhotographicSeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying VLPhotographicSeriesPseudo " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (VLImage && !VLImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying VLImage " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VideoPhotographicImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_OphthalmicPhotography8BitImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "OphthalmicPhotography8BitImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotographySeries && !OphthalmicPhotographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying OphthalmicPhotographySeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotographyImage && !OphthalmicPhotographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying OphthalmicPhotographyImage " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotography8BitImagePseudo && !OphthalmicPhotography8BitImagePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying OphthalmicPhotography8BitImagePseudo " << (success ? "success" : "failure") << endl;

	if (OcularRegionImaged && !OcularRegionImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying OcularRegionImaged " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotographyAcquisitionParameters && !OphthalmicPhotographyAcquisitionParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying OphthalmicPhotographyAcquisitionParameters " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotographicParameters && !OphthalmicPhotographicParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying OphthalmicPhotographicParameters " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography8BitImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_OphthalmicPhotography16BitImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "OphthalmicPhotography16BitImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotographySeries && !OphthalmicPhotographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying OphthalmicPhotographySeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (Cine && !Cine->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying Cine " << (success ? "success" : "failure") << endl;

	if (MultiFrame && !MultiFrame->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying MultiFrame " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotographyImage && !OphthalmicPhotographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying OphthalmicPhotographyImage " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotography16BitImagePseudo && !OphthalmicPhotography16BitImagePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying OphthalmicPhotography16BitImagePseudo " << (success ? "success" : "failure") << endl;

	if (OcularRegionImaged && !OcularRegionImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying OcularRegionImaged " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotographyAcquisitionParameters && !OphthalmicPhotographyAcquisitionParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying OphthalmicPhotographyAcquisitionParameters " << (success ? "success" : "failure") << endl;

	if (OphthalmicPhotographicParameters && !OphthalmicPhotographicParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying OphthalmicPhotographicParameters " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicPhotography16BitImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_StereometricRelationship::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "StereometricRelationship";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (StereometricSeries && !StereometricSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying StereometricSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (StereometricRelationship && !StereometricRelationship->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying StereometricRelationship " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "StereometricRelationship success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_OphthalmicTomographyImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "OphthalmicTomographyImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (OphthalmicTomographySeries && !OphthalmicTomographySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying OphthalmicTomographySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForOphthalmicTomography && !MultiFrameFunctionalGroupsForOphthalmicTomography->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying MultiFrameFunctionalGroupsForOphthalmicTomography " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (OphthalmicTomographyImage && !OphthalmicTomographyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying OphthalmicTomographyImage " << (success ? "success" : "failure") << endl;

	if (OphthalmicTomographyAcquisitionParameters && !OphthalmicTomographyAcquisitionParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying OphthalmicTomographyAcquisitionParameters " << (success ? "success" : "failure") << endl;

	if (OphthalmicTomographyParameters && !OphthalmicTomographyParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying OphthalmicTomographyParameters " << (success ? "success" : "failure") << endl;

	if (OcularRegionImaged && !OcularRegionImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying OcularRegionImaged " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicTomographyImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_OphthalmicOpticalCoherenceTomographyEnFaceImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "OphthalmicOpticalCoherenceTomographyEnFaceImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (OphthalmicTomographyEnFaceSeries && !OphthalmicTomographyEnFaceSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying OphthalmicTomographyEnFaceSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (PaletteColorLookupTable && !PaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying PaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (OphthalmicOpticalCoherenceTomographyEnFaceImage && !OphthalmicOpticalCoherenceTomographyEnFaceImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying OphthalmicOpticalCoherenceTomographyEnFaceImage " << (success ? "success" : "failure") << endl;

	if (OcularRegionImaged && !OcularRegionImaged->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying OcularRegionImaged " << (success ? "success" : "failure") << endl;

	if (OphthalmicOpticalCoherenceTomographyEnFaceImageQualityRating && !OphthalmicOpticalCoherenceTomographyEnFaceImageQualityRating->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying OphthalmicOpticalCoherenceTomographyEnFaceImageQualityRating " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyEnFaceImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (OphthalmicTomographyBscanVolumeAnalysisSeries && !OphthalmicTomographyBscanVolumeAnalysisSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying OphthalmicTomographyBscanVolumeAnalysisSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysisImage && !OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysisImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysisImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForOphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis && !MultiFrameFunctionalGroupsForOphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying MultiFrameFunctionalGroupsForOphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicOpticalCoherenceTomographyBscanVolumeAnalysis success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VLWholeSlideMicroscopyImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VLWholeSlideMicroscopyImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (WholeSlideMicroscopySeries && !WholeSlideMicroscopySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying WholeSlideMicroscopySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (MultiResolutionPyramid && !MultiResolutionPyramid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying MultiResolutionPyramid " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (MicroscopeSlideLayerTileOrganization && !MicroscopeSlideLayerTileOrganization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying MicroscopeSlideLayerTileOrganization " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForWholeSlideMicroscopy && !MultiFrameFunctionalGroupsForWholeSlideMicroscopy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying MultiFrameFunctionalGroupsForWholeSlideMicroscopy " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (WholeSlideMicroscopyImage && !WholeSlideMicroscopyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying WholeSlideMicroscopyImage " << (success ? "success" : "failure") << endl;

	if (OpticalPath && !OpticalPath->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying OpticalPath " << (success ? "success" : "failure") << endl;

	if (SlideLabel && !SlideLabel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying SlideLabel " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VLWholeSlideMicroscopyImageWG26SP2025::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VLWholeSlideMicroscopyImageWG26SP2025";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (WholeSlideMicroscopySeries && !WholeSlideMicroscopySeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying WholeSlideMicroscopySeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (MultiResolutionPyramid && !MultiResolutionPyramid->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying MultiResolutionPyramid " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (MicroscopeSlideLayerTileOrganization && !MicroscopeSlideLayerTileOrganization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying MicroscopeSlideLayerTileOrganization " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForWholeSlideMicroscopy && !MultiFrameFunctionalGroupsForWholeSlideMicroscopy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying MultiFrameFunctionalGroupsForWholeSlideMicroscopy " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (WholeSlideMicroscopyImage && !WholeSlideMicroscopyImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying WholeSlideMicroscopyImage " << (success ? "success" : "failure") << endl;

	if (OpticalPath && !OpticalPath->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying OpticalPath " << (success ? "success" : "failure") << endl;

	if (SlideLabel && !SlideLabel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying SlideLabel " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	if (WG26SP2025WSI && !WG26SP2025WSI->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VLWholeSlideMicroscopyImageWG26SP2025 success after verifying WG26SP2025WSI " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_LensometryMeasurements::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "LensometryMeasurements";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (LensometryMeasurementsSeries && !LensometryMeasurementsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying LensometryMeasurementsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralOphthalmicRefractiveMeasurements && !GeneralOphthalmicRefractiveMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying GeneralOphthalmicRefractiveMeasurements " << (success ? "success" : "failure") << endl;

	if (LensometryMeasurements && !LensometryMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying LensometryMeasurements " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LensometryMeasurements success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_AutorefractionMeasurements::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "AutorefractionMeasurements";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (AutorefractionMeasurementsSeries && !AutorefractionMeasurementsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying AutorefractionMeasurementsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralOphthalmicRefractiveMeasurements && !GeneralOphthalmicRefractiveMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying GeneralOphthalmicRefractiveMeasurements " << (success ? "success" : "failure") << endl;

	if (AutorefractionMeasurements && !AutorefractionMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying AutorefractionMeasurements " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AutorefractionMeasurements success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_KeratometryMeasurements::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "KeratometryMeasurements";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (KeratometryMeasurementsSeries && !KeratometryMeasurementsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying KeratometryMeasurementsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralOphthalmicRefractiveMeasurements && !GeneralOphthalmicRefractiveMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying GeneralOphthalmicRefractiveMeasurements " << (success ? "success" : "failure") << endl;

	if (KeratometryMeasurements && !KeratometryMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying KeratometryMeasurements " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeratometryMeasurements success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_SubjectiveRefractionMeasurements::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "SubjectiveRefractionMeasurements";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (SubjectiveRefractionMeasurementsSeries && !SubjectiveRefractionMeasurementsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying SubjectiveRefractionMeasurementsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralOphthalmicRefractiveMeasurements && !GeneralOphthalmicRefractiveMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying GeneralOphthalmicRefractiveMeasurements " << (success ? "success" : "failure") << endl;

	if (SubjectiveRefractionMeasurements && !SubjectiveRefractionMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying SubjectiveRefractionMeasurements " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SubjectiveRefractionMeasurements success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_VisualAcuityMeasurements::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "VisualAcuityMeasurements";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (VisualAcuityMeasurementsSeries && !VisualAcuityMeasurementsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying VisualAcuityMeasurementsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralOphthalmicRefractiveMeasurements && !GeneralOphthalmicRefractiveMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying GeneralOphthalmicRefractiveMeasurements " << (success ? "success" : "failure") << endl;

	if (VisualAcuityMeasurements && !VisualAcuityMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying VisualAcuityMeasurements " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "VisualAcuityMeasurements success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_OphthalmicAxialMeasurements::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "OphthalmicAxialMeasurements";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (OphthalmicAxialMeasurementsSeries && !OphthalmicAxialMeasurementsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying OphthalmicAxialMeasurementsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (OphthalmicAxialMeasurements && !OphthalmicAxialMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying OphthalmicAxialMeasurements " << (success ? "success" : "failure") << endl;

	if (GeneralOphthalmicRefractiveMeasurements && !GeneralOphthalmicRefractiveMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying GeneralOphthalmicRefractiveMeasurements " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicAxialMeasurements success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_IntraocularLensCalculations::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "IntraocularLensCalculations";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (IntraocularLensCalculationsSeries && !IntraocularLensCalculationsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying IntraocularLensCalculationsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (IntraocularLensCalculations && !IntraocularLensCalculations->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying IntraocularLensCalculations " << (success ? "success" : "failure") << endl;

	if (GeneralOphthalmicRefractiveMeasurements && !GeneralOphthalmicRefractiveMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying GeneralOphthalmicRefractiveMeasurements " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "IntraocularLensCalculations success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_OphthalmicVisualFieldStaticPerimetryMeasurements::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "OphthalmicVisualFieldStaticPerimetryMeasurements";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (VisualFieldStaticPerimetryMeasurementsSeries && !VisualFieldStaticPerimetryMeasurementsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying VisualFieldStaticPerimetryMeasurementsSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (VisualFieldStaticPerimetryTestParameters && !VisualFieldStaticPerimetryTestParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying VisualFieldStaticPerimetryTestParameters " << (success ? "success" : "failure") << endl;

	if (VisualFieldStaticPerimetryTestReliability && !VisualFieldStaticPerimetryTestReliability->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying VisualFieldStaticPerimetryTestReliability " << (success ? "success" : "failure") << endl;

	if (VisualFieldStaticPerimetryTestMeasurements && !VisualFieldStaticPerimetryTestMeasurements->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying VisualFieldStaticPerimetryTestMeasurements " << (success ? "success" : "failure") << endl;

	if (VisualFieldStaticPerimetryTestResults && !VisualFieldStaticPerimetryTestResults->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying VisualFieldStaticPerimetryTestResults " << (success ? "success" : "failure") << endl;

	if (OphthalmicPatientClinicalInformationandTestLensParameters && !OphthalmicPatientClinicalInformationandTestLensParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying OphthalmicPatientClinicalInformationandTestLensParameters " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "OphthalmicVisualFieldStaticPerimetryMeasurements success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_DermoscopicPhotographyImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "DermoscopicPhotographyImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (DermoscopyPhotographySeriesPseudo && !DermoscopyPhotographySeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying DermoscopyPhotographySeriesPseudo " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (VLPhotographicEquipment && !VLPhotographicEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying VLPhotographicEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (VLImage && !VLImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying VLImage " << (success ? "success" : "failure") << endl;

	if (VLPhotographicAcquisition && !VLPhotographicAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying VLPhotographicAcquisition " << (success ? "success" : "failure") << endl;

	if (DermoscopicImage && !DermoscopicImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying DermoscopicImage " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (CheckSingleFramePseudo && !CheckSingleFramePseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying CheckSingleFramePseudo " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "DermoscopicPhotographyImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MicroscopyBulkSimpleAnnotations::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MicroscopyBulkSimpleAnnotations";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (MicroscopyBulkSimpleAnnotationsSeries && !MicroscopyBulkSimpleAnnotationsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying MicroscopyBulkSimpleAnnotationsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (MicroscopyBulkSimpleAnnotations && !MicroscopyBulkSimpleAnnotations->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying MicroscopyBulkSimpleAnnotations " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotations success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MicroscopyBulkSimpleAnnotationsWG26SP2025::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MicroscopyBulkSimpleAnnotationsWG26SP2025";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (MicroscopyBulkSimpleAnnotationsSeries && !MicroscopyBulkSimpleAnnotationsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying MicroscopyBulkSimpleAnnotationsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (MicroscopyBulkSimpleAnnotations && !MicroscopyBulkSimpleAnnotations->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying MicroscopyBulkSimpleAnnotations " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (WG26SP2025ANN && !WG26SP2025ANN->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MicroscopyBulkSimpleAnnotationsWG26SP2025 success after verifying WG26SP2025ANN " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_BasicVoice::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "BasicVoice";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (WaveformIdentification && !WaveformIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying WaveformIdentification " << (success ? "success" : "failure") << endl;

	if (Waveform && !Waveform->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying Waveform " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (WaveformAnnotation && !WaveformAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying WaveformAnnotation " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicVoice success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_TwelveLeadECG::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "TwelveLeadECG";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (WaveformIdentification && !WaveformIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying WaveformIdentification " << (success ? "success" : "failure") << endl;

	if (Waveform && !Waveform->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying Waveform " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (WaveformAnnotation && !WaveformAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying WaveformAnnotation " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TwelveLeadECG success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_GeneralECG::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "GeneralECG";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (WaveformIdentification && !WaveformIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying WaveformIdentification " << (success ? "success" : "failure") << endl;

	if (Waveform && !Waveform->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying Waveform " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (WaveformAnnotation && !WaveformAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying WaveformAnnotation " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GeneralECG success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_AmbulatoryECG::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "AmbulatoryECG";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (WaveformIdentification && !WaveformIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying WaveformIdentification " << (success ? "success" : "failure") << endl;

	if (Waveform && !Waveform->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying Waveform " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (WaveformAnnotation && !WaveformAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying WaveformAnnotation " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AmbulatoryECG success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_HemodynamicWaveform::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "HemodynamicWaveform";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (WaveformIdentification && !WaveformIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying WaveformIdentification " << (success ? "success" : "failure") << endl;

	if (Waveform && !Waveform->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying Waveform " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (WaveformAnnotation && !WaveformAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying WaveformAnnotation " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HemodynamicWaveform success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_CardiacElectrophysiologyWaveform::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "CardiacElectrophysiologyWaveform";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (WaveformIdentification && !WaveformIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying WaveformIdentification " << (success ? "success" : "failure") << endl;

	if (Waveform && !Waveform->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying Waveform " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (WaveformAnnotation && !WaveformAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying WaveformAnnotation " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "CardiacElectrophysiologyWaveform success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_BasicTextSR::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "BasicTextSR";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicTextSR success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EnhancedSR::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EnhancedSR";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedSR success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_ComprehensiveSR::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "ComprehensiveSR";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ComprehensiveSR success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_Comprehensive3DSR::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "Comprehensive3DSR";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "Comprehensive3DSR success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_KeyObjectSelectionDocument::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "KeyObjectSelectionDocument";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (KeyObjectDocumentSeries && !KeyObjectDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying KeyObjectDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (KeyObjectDocument && !KeyObjectDocument->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying KeyObjectDocument " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocument success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_KeyObjectSelectionDocumentIHEXDSIManifest::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "KeyObjectSelectionDocumentIHEXDSIManifest";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (KeyObjectDocumentSeries && !KeyObjectDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying KeyObjectDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (KeyObjectDocument && !KeyObjectDocument->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying KeyObjectDocument " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (IHEXDSIManifestProfile && !IHEXDSIManifestProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "KeyObjectSelectionDocumentIHEXDSIManifest success after verifying IHEXDSIManifestProfile " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MammographyCADSR::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MammographyCADSR";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MammographyCADSR success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_ChestCADSR::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "ChestCADSR";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ChestCADSR success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_ProcedureLog::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "ProcedureLog";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ProcedureLog success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_XRayRadiationDoseSR::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "XRayRadiationDoseSR";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSR success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_XRayRadiationDoseSRIHEREM::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "XRayRadiationDoseSRIHEREM";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (IHEREMProfile && !IHEREMProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "XRayRadiationDoseSRIHEREM success after verifying IHEREMProfile " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RadiopharmaceuticalRadiationDoseSR::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RadiopharmaceuticalRadiationDoseSR";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RadiopharmaceuticalRadiationDoseSR success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_SpectaclePrescriptionReport::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "SpectaclePrescriptionReport";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "SpectaclePrescriptionReport success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_AcquisitionContextSR::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "AcquisitionContextSR";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (SRDocumentSeries && !SRDocumentSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying SRDocumentSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (SRDocumentGeneral && !SRDocumentGeneral->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying SRDocumentGeneral " << (success ? "success" : "failure") << endl;

	if (SRDocumentContent && !SRDocumentContent->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying SRDocumentContent " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AcquisitionContextSR success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_GrayscaleSoftcopyPresentationState::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "GrayscaleSoftcopyPresentationState";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (PresentationSeries && !PresentationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying PresentationSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (PresentationStateIdentification && !PresentationStateIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying PresentationStateIdentification " << (success ? "success" : "failure") << endl;

	if (PresentationStateRelationship && !PresentationStateRelationship->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying PresentationStateRelationship " << (success ? "success" : "failure") << endl;

	if (PresentationStateShutter && !PresentationStateShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying PresentationStateShutter " << (success ? "success" : "failure") << endl;

	if (PresentationStateMask && !PresentationStateMask->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying PresentationStateMask " << (success ? "success" : "failure") << endl;

	if (Mask && !Mask->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying Mask " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (BitmapDisplayShutter && !BitmapDisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying BitmapDisplayShutter " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (OverlayActivation && !OverlayActivation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying OverlayActivation " << (success ? "success" : "failure") << endl;

	if (DisplayedArea && !DisplayedArea->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying DisplayedArea " << (success ? "success" : "failure") << endl;

	if (GraphicAnnotation && !GraphicAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying GraphicAnnotation " << (success ? "success" : "failure") << endl;

	if (SpatialTransformation && !SpatialTransformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying SpatialTransformation " << (success ? "success" : "failure") << endl;

	if (GraphicLayer && !GraphicLayer->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying GraphicLayer " << (success ? "success" : "failure") << endl;

	if (GraphicGroup && !GraphicGroup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying GraphicGroup " << (success ? "success" : "failure") << endl;

	if (ModalityLUT && !ModalityLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying ModalityLUT " << (success ? "success" : "failure") << endl;

	if (SoftcopyVOILUT && !SoftcopyVOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying SoftcopyVOILUT " << (success ? "success" : "failure") << endl;

	if (SoftcopyPresentationLUT && !SoftcopyPresentationLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying SoftcopyPresentationLUT " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "GrayscaleSoftcopyPresentationState success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_ColorSoftcopyPresentationState::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "ColorSoftcopyPresentationState";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (PresentationSeries && !PresentationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying PresentationSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (PresentationStateIdentification && !PresentationStateIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying PresentationStateIdentification " << (success ? "success" : "failure") << endl;

	if (PresentationStateRelationship && !PresentationStateRelationship->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying PresentationStateRelationship " << (success ? "success" : "failure") << endl;

	if (PresentationStateShutter && !PresentationStateShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying PresentationStateShutter " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (BitmapDisplayShutter && !BitmapDisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying BitmapDisplayShutter " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (OverlayActivation && !OverlayActivation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying OverlayActivation " << (success ? "success" : "failure") << endl;

	if (DisplayedArea && !DisplayedArea->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying DisplayedArea " << (success ? "success" : "failure") << endl;

	if (GraphicAnnotation && !GraphicAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying GraphicAnnotation " << (success ? "success" : "failure") << endl;

	if (SpatialTransformation && !SpatialTransformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying SpatialTransformation " << (success ? "success" : "failure") << endl;

	if (GraphicLayer && !GraphicLayer->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying GraphicLayer " << (success ? "success" : "failure") << endl;

	if (GraphicGroup && !GraphicGroup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying GraphicGroup " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorSoftcopyPresentationState success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_PseudoColorSoftcopyPresentationState::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "PseudoColorSoftcopyPresentationState";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (PresentationSeries && !PresentationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying PresentationSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (PresentationStateIdentification && !PresentationStateIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying PresentationStateIdentification " << (success ? "success" : "failure") << endl;

	if (PresentationStateRelationship && !PresentationStateRelationship->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying PresentationStateRelationship " << (success ? "success" : "failure") << endl;

	if (PresentationStateShutter && !PresentationStateShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying PresentationStateShutter " << (success ? "success" : "failure") << endl;

	if (PresentationStateMask && !PresentationStateMask->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying PresentationStateMask " << (success ? "success" : "failure") << endl;

	if (Mask && !Mask->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying Mask " << (success ? "success" : "failure") << endl;

	if (DisplayShutter && !DisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying DisplayShutter " << (success ? "success" : "failure") << endl;

	if (BitmapDisplayShutter && !BitmapDisplayShutter->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying BitmapDisplayShutter " << (success ? "success" : "failure") << endl;

	if (OverlayPlane && !OverlayPlane->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying OverlayPlane " << (success ? "success" : "failure") << endl;

	if (OverlayActivation && !OverlayActivation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying OverlayActivation " << (success ? "success" : "failure") << endl;

	if (DisplayedArea && !DisplayedArea->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying DisplayedArea " << (success ? "success" : "failure") << endl;

	if (GraphicAnnotation && !GraphicAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying GraphicAnnotation " << (success ? "success" : "failure") << endl;

	if (SpatialTransformation && !SpatialTransformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying SpatialTransformation " << (success ? "success" : "failure") << endl;

	if (GraphicLayer && !GraphicLayer->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying GraphicLayer " << (success ? "success" : "failure") << endl;

	if (GraphicGroup && !GraphicGroup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying GraphicGroup " << (success ? "success" : "failure") << endl;

	if (ModalityLUT && !ModalityLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying ModalityLUT " << (success ? "success" : "failure") << endl;

	if (SoftcopyVOILUT && !SoftcopyVOILUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying SoftcopyVOILUT " << (success ? "success" : "failure") << endl;

	if (PaletteColorLookupTable && !PaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying PaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PseudoColorSoftcopyPresentationState success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_BlendingSoftcopyPresentationState::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "BlendingSoftcopyPresentationState";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (PresentationSeries && !PresentationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying PresentationSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (PresentationStateIdentification && !PresentationStateIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying PresentationStateIdentification " << (success ? "success" : "failure") << endl;

	if (PresentationStateBlending && !PresentationStateBlending->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying PresentationStateBlending " << (success ? "success" : "failure") << endl;

	if (DisplayedArea && !DisplayedArea->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying DisplayedArea " << (success ? "success" : "failure") << endl;

	if (GraphicAnnotation && !GraphicAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying GraphicAnnotation " << (success ? "success" : "failure") << endl;

	if (SpatialTransformation && !SpatialTransformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying SpatialTransformation " << (success ? "success" : "failure") << endl;

	if (GraphicLayer && !GraphicLayer->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying GraphicLayer " << (success ? "success" : "failure") << endl;

	if (GraphicGroup && !GraphicGroup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying GraphicGroup " << (success ? "success" : "failure") << endl;

	if (PaletteColorLookupTable && !PaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying PaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BlendingSoftcopyPresentationState success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_AdvancedBlendingSoftcopyPresentationState::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "AdvancedBlendingSoftcopyPresentationState";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (PresentationSeries && !PresentationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying PresentationSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (PresentationStateIdentification && !PresentationStateIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying PresentationStateIdentification " << (success ? "success" : "failure") << endl;

	if (AdvancedBlendingPresentationState && !AdvancedBlendingPresentationState->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying AdvancedBlendingPresentationState " << (success ? "success" : "failure") << endl;

	if (AdvancedBlendingPresentationStateDisplay && !AdvancedBlendingPresentationStateDisplay->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying AdvancedBlendingPresentationStateDisplay " << (success ? "success" : "failure") << endl;

	if (DisplayedArea && !DisplayedArea->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying DisplayedArea " << (success ? "success" : "failure") << endl;

	if (GraphicAnnotation && !GraphicAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying GraphicAnnotation " << (success ? "success" : "failure") << endl;

	if (SpatialTransformation && !SpatialTransformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying SpatialTransformation " << (success ? "success" : "failure") << endl;

	if (GraphicLayer && !GraphicLayer->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying GraphicLayer " << (success ? "success" : "failure") << endl;

	if (GraphicGroup && !GraphicGroup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying GraphicGroup " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationState success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (PresentationSeries && !PresentationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying PresentationSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (PresentationStateIdentification && !PresentationStateIdentification->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying PresentationStateIdentification " << (success ? "success" : "failure") << endl;

	if (AdvancedBlendingPresentationStateIDCVOIPaletteOpticalPath && !AdvancedBlendingPresentationStateIDCVOIPaletteOpticalPath->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying AdvancedBlendingPresentationStateIDCVOIPaletteOpticalPath " << (success ? "success" : "failure") << endl;

	if (AdvancedBlendingPresentationStateDisplay && !AdvancedBlendingPresentationStateDisplay->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying AdvancedBlendingPresentationStateDisplay " << (success ? "success" : "failure") << endl;

	if (DisplayedArea && !DisplayedArea->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying DisplayedArea " << (success ? "success" : "failure") << endl;

	if (GraphicAnnotation && !GraphicAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying GraphicAnnotation " << (success ? "success" : "failure") << endl;

	if (SpatialTransformation && !SpatialTransformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying SpatialTransformation " << (success ? "success" : "failure") << endl;

	if (GraphicLayer && !GraphicLayer->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying GraphicLayer " << (success ? "success" : "failure") << endl;

	if (GraphicGroup && !GraphicGroup->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying GraphicGroup " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "AdvancedBlendingSoftcopyPresentationStateIDCVOIPaletteOpticalPath success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_HangingProtocol::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "HangingProtocol";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HangingProtocol success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (HangingProtocolDefinition && !HangingProtocolDefinition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HangingProtocol success after verifying HangingProtocolDefinition " << (success ? "success" : "failure") << endl;

	if (HangingProtocolEnvironment && !HangingProtocolEnvironment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HangingProtocol success after verifying HangingProtocolEnvironment " << (success ? "success" : "failure") << endl;

	if (HangingProtocolDisplay && !HangingProtocolDisplay->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HangingProtocol success after verifying HangingProtocolDisplay " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "HangingProtocol success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_ColorPalette::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "ColorPalette";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorPalette success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (ColorPaletteDefinition && !ColorPaletteDefinition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorPalette success after verifying ColorPaletteDefinition " << (success ? "success" : "failure") << endl;

	if (PaletteColorLookupTable && !PaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorPalette success after verifying PaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorPalette success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "ColorPalette success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_BasicStructuredDisplay::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "BasicStructuredDisplay";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (PresentationSeries && !PresentationSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying PresentationSeries " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (StructuredDisplay && !StructuredDisplay->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying StructuredDisplay " << (success ? "success" : "failure") << endl;

	if (StructuredDisplayImageBox && !StructuredDisplayImageBox->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying StructuredDisplayImageBox " << (success ? "success" : "failure") << endl;

	if (StructuredDisplayAnnotation && !StructuredDisplayAnnotation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying StructuredDisplayAnnotation " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "BasicStructuredDisplay success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EnhancedMRImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EnhancedMRImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (MRSeries && !MRSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying MRSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForEnhancedMRImage && !MultiFrameFunctionalGroupsForEnhancedMRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying MultiFrameFunctionalGroupsForEnhancedMRImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (BulkMotionSynchronization && !BulkMotionSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying BulkMotionSynchronization " << (success ? "success" : "failure") << endl;

	if (SupplementalPaletteColorLUT && !SupplementalPaletteColorLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying SupplementalPaletteColorLUT " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedMRImage && !EnhancedMRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying EnhancedMRImage " << (success ? "success" : "failure") << endl;

	if (MRPulseSequence && !MRPulseSequence->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying MRPulseSequence " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EnhancedMRColorImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EnhancedMRColorImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (MRSeries && !MRSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying MRSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForEnhancedMRImage && !MultiFrameFunctionalGroupsForEnhancedMRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying MultiFrameFunctionalGroupsForEnhancedMRImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (BulkMotionSynchronization && !BulkMotionSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying BulkMotionSynchronization " << (success ? "success" : "failure") << endl;

	if (SupplementalPaletteColorLUT && !SupplementalPaletteColorLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying SupplementalPaletteColorLUT " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedMRImage && !EnhancedMRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying EnhancedMRImage " << (success ? "success" : "failure") << endl;

	if (MRPulseSequence && !MRPulseSequence->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying MRPulseSequence " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedMRColorImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_MRSpectroscopy::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "MRSpectroscopy";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (MRSeries && !MRSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying MRSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForMRSpectroscopy && !MultiFrameFunctionalGroupsForMRSpectroscopy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying MultiFrameFunctionalGroupsForMRSpectroscopy " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (BulkMotionSynchronization && !BulkMotionSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying BulkMotionSynchronization " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (MRSpectroscopy && !MRSpectroscopy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying MRSpectroscopy " << (success ? "success" : "failure") << endl;

	if (MRSpectroscopyPulseSequence && !MRSpectroscopyPulseSequence->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying MRSpectroscopyPulseSequence " << (success ? "success" : "failure") << endl;

	if (MRSpectroscopyData && !MRSpectroscopyData->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying MRSpectroscopyData " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "MRSpectroscopy success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_RawData::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "RawData";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (RawData && !RawData->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying RawData " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "RawData success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_LegacyConvertedEnhancedMRImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "LegacyConvertedEnhancedMRImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (MRSeries && !MRSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying MRSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForLegacyConvertedEnhancedMRImage && !MultiFrameFunctionalGroupsForLegacyConvertedEnhancedMRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying MultiFrameFunctionalGroupsForLegacyConvertedEnhancedMRImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (BulkMotionSynchronization && !BulkMotionSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying BulkMotionSynchronization " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedMRImage && !EnhancedMRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying EnhancedMRImage " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedMRImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_PrivatePixelMedLegacyConvertedEnhancedMRImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "PrivatePixelMedLegacyConvertedEnhancedMRImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (MRSeries && !MRSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying MRSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForPrivatePixelMedLegacyConvertedEnhancedMRImage && !MultiFrameFunctionalGroupsForPrivatePixelMedLegacyConvertedEnhancedMRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying MultiFrameFunctionalGroupsForPrivatePixelMedLegacyConvertedEnhancedMRImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (BulkMotionSynchronization && !BulkMotionSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying BulkMotionSynchronization " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedMRImage && !EnhancedMRImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying EnhancedMRImage " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedMRImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_TractographyResults::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "TractographyResults";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (TractographyResultsSeries && !TractographyResultsSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying TractographyResultsSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (TractographyResults && !TractographyResults->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying TractographyResults " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "TractographyResults success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EnhancedCTImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EnhancedCTImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (CTSeries && !CTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying CTSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForEnhancedCTImage && !MultiFrameFunctionalGroupsForEnhancedCTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying MultiFrameFunctionalGroupsForEnhancedCTImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (SupplementalPaletteColorLUT && !SupplementalPaletteColorLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying SupplementalPaletteColorLUT " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedCTImage && !EnhancedCTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying EnhancedCTImage " << (success ? "success" : "failure") << endl;

	if (EnhancedMultienergyCTAcquisition && !EnhancedMultienergyCTAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying EnhancedMultienergyCTAcquisition " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedCTImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_LegacyConvertedEnhancedCTImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "LegacyConvertedEnhancedCTImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (CTSeries && !CTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying CTSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForLegacyConvertedEnhancedCTImage && !MultiFrameFunctionalGroupsForLegacyConvertedEnhancedCTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying MultiFrameFunctionalGroupsForLegacyConvertedEnhancedCTImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedCTImage && !EnhancedCTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying EnhancedCTImage " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "LegacyConvertedEnhancedCTImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_PrivatePixelMedLegacyConvertedEnhancedCTImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "PrivatePixelMedLegacyConvertedEnhancedCTImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (CTSeries && !CTSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying CTSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (ContrastBolus && !ContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying ContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForPrivatePixelMedLegacyConvertedEnhancedCTImage && !MultiFrameFunctionalGroupsForPrivatePixelMedLegacyConvertedEnhancedCTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying MultiFrameFunctionalGroupsForPrivatePixelMedLegacyConvertedEnhancedCTImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedCTImage && !EnhancedCTImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying EnhancedCTImage " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PrivatePixelMedLegacyConvertedEnhancedCTImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EnhancedUltrasoundVolume::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EnhancedUltrasoundVolume";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedUSSeries && !EnhancedUSSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying EnhancedUSSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (UltrasoundFrameOfReference && !UltrasoundFrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying UltrasoundFrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForEnhancedUSVolume && !MultiFrameFunctionalGroupsForEnhancedUSVolume->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying MultiFrameFunctionalGroupsForEnhancedUSVolume " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying Device " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedPaletteColorLookupTable && !EnhancedPaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying EnhancedPaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (EnhancedUSImage && !EnhancedUSImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying EnhancedUSImage " << (success ? "success" : "failure") << endl;

	if (IVUSImage && !IVUSImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying IVUSImage " << (success ? "success" : "failure") << endl;

	if (ExcludedIntervals && !ExcludedIntervals->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying ExcludedIntervals " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolume success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_EnhancedUltrasoundVolumeQTUS::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "EnhancedUltrasoundVolumeQTUS";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (QTUSEnhancedUltrasoundVolumeProfileStudy && !QTUSEnhancedUltrasoundVolumeProfileStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying QTUSEnhancedUltrasoundVolumeProfileStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedUSSeries && !EnhancedUSSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying EnhancedUSSeries " << (success ? "success" : "failure") << endl;

	if (QTUSEnhancedUltrasoundVolumeProfileSeries && !QTUSEnhancedUltrasoundVolumeProfileSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying QTUSEnhancedUltrasoundVolumeProfileSeries " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (UltrasoundFrameOfReference && !UltrasoundFrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying UltrasoundFrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (QTUSEnhancedUltrasoundVolumeProfileFrameOfReference && !QTUSEnhancedUltrasoundVolumeProfileFrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying QTUSEnhancedUltrasoundVolumeProfileFrameOfReference " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (QTUSEnhancedUltrasoundVolumeProfileEquipment && !QTUSEnhancedUltrasoundVolumeProfileEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying QTUSEnhancedUltrasoundVolumeProfileEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralAcquisition && !GeneralAcquisition->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying GeneralAcquisition " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForEnhancedUSVolume && !MultiFrameFunctionalGroupsForEnhancedUSVolume->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying MultiFrameFunctionalGroupsForEnhancedUSVolume " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (CardiacSynchronization && !CardiacSynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying CardiacSynchronization " << (success ? "success" : "failure") << endl;

	if (RespiratorySynchronization && !RespiratorySynchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying RespiratorySynchronization " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying Device " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (EnhancedPaletteColorLookupTable && !EnhancedPaletteColorLookupTable->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying EnhancedPaletteColorLookupTable " << (success ? "success" : "failure") << endl;

	if (EnhancedUSImage && !EnhancedUSImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying EnhancedUSImage " << (success ? "success" : "failure") << endl;

	if (IVUSImage && !IVUSImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying IVUSImage " << (success ? "success" : "failure") << endl;

	if (ExcludedIntervals && !ExcludedIntervals->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying ExcludedIntervals " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	if (QTUSEnhancedUltrasoundVolumeProfileInstance && !QTUSEnhancedUltrasoundVolumeProfileInstance->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "EnhancedUltrasoundVolumeQTUS success after verifying QTUSEnhancedUltrasoundVolumeProfileInstance " << (success ? "success" : "failure") << endl;

	return success;
}

bool
CompositeIOD_PhotoacousticImage::verify(AttributeList *list,bool verbose,bool newformat,bool allpffgitems,TextOutputStream& log,ElementDictionary *dict) const
{
	const char *iodcomp = "PhotoacousticImage";
	(void)iodcomp;  // Quiets compiler in case iodcomp empty
	(void)list;
	(void)verbose;
	(void)log;
	(void)dict;
	Assert(list);
	Assert(dict);

	bool success=true;


	if (verbose)
		log << MMsgDC(Verifying) << " " << MMsgDC(CompositeInformationObject) << " " << iodcomp << endl;

	if (FileMetaInformation && !FileMetaInformation->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying FileMetaInformation " << (success ? "success" : "failure") << endl;

	if (Patient && !Patient->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying Patient " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSubject && !ClinicalTrialSubject->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying ClinicalTrialSubject " << (success ? "success" : "failure") << endl;

	if (GeneralStudy && !GeneralStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying GeneralStudy " << (success ? "success" : "failure") << endl;

	if (PatientStudy && !PatientStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying PatientStudy " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialStudy && !ClinicalTrialStudy->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying ClinicalTrialStudy " << (success ? "success" : "failure") << endl;

	if (GeneralSeries && !GeneralSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying GeneralSeries " << (success ? "success" : "failure") << endl;

	if (ClinicalTrialSeries && !ClinicalTrialSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying ClinicalTrialSeries " << (success ? "success" : "failure") << endl;

	if (EnhancedSeries && !EnhancedSeries->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying EnhancedSeries " << (success ? "success" : "failure") << endl;

	if (PhotoacousticSeriesPseudo && !PhotoacousticSeriesPseudo->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying PhotoacousticSeriesPseudo " << (success ? "success" : "failure") << endl;

	if (FrameOfReference && !FrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying FrameOfReference " << (success ? "success" : "failure") << endl;

	if (UltrasoundFrameOfReference && !UltrasoundFrameOfReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying UltrasoundFrameOfReference " << (success ? "success" : "failure") << endl;

	if (Synchronization && !Synchronization->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying Synchronization " << (success ? "success" : "failure") << endl;

	if (GeneralEquipment && !GeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying GeneralEquipment " << (success ? "success" : "failure") << endl;

	if (EnhancedGeneralEquipment && !EnhancedGeneralEquipment->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying EnhancedGeneralEquipment " << (success ? "success" : "failure") << endl;

	if (GeneralImage && !GeneralImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying GeneralImage " << (success ? "success" : "failure") << endl;

	if (GeneralReference && !GeneralReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying GeneralReference " << (success ? "success" : "failure") << endl;

	if (ImagePixel && !ImagePixel->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying ImagePixel " << (success ? "success" : "failure") << endl;

	if (EnhancedContrastBolus && !EnhancedContrastBolus->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying EnhancedContrastBolus " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsCommon && !MultiFrameFunctionalGroupsCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying MultiFrameFunctionalGroupsCommon " << (success ? "success" : "failure") << endl;

	if (MultiFrameFunctionalGroupsForPhotoacousticImage && !MultiFrameFunctionalGroupsForPhotoacousticImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying MultiFrameFunctionalGroupsForPhotoacousticImage " << (success ? "success" : "failure") << endl;

	if (MultiFrameDimension && !MultiFrameDimension->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying MultiFrameDimension " << (success ? "success" : "failure") << endl;

	if (Device && !Device->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying Device " << (success ? "success" : "failure") << endl;

	if (AcquisitionContext && !AcquisitionContext->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying AcquisitionContext " << (success ? "success" : "failure") << endl;

	if (Specimen && !Specimen->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying Specimen " << (success ? "success" : "failure") << endl;

	if (SupplementalPaletteColorLUT && !SupplementalPaletteColorLUT->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying SupplementalPaletteColorLUT " << (success ? "success" : "failure") << endl;

	if (PhotoacousticImage && !PhotoacousticImage->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying PhotoacousticImage " << (success ? "success" : "failure") << endl;

	if (PhotoacousticAcquisitionParameters && !PhotoacousticAcquisitionParameters->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying PhotoacousticAcquisitionParameters " << (success ? "success" : "failure") << endl;

	if (PhotoacousticTransducer && !PhotoacousticTransducer->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying PhotoacousticTransducer " << (success ? "success" : "failure") << endl;

	if (PhotoacousticReconstruction && !PhotoacousticReconstruction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying PhotoacousticReconstruction " << (success ? "success" : "failure") << endl;

	if (ICCProfile && !ICCProfile->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying ICCProfile " << (success ? "success" : "failure") << endl;

	if (SOPCommon && !SOPCommon->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying SOPCommon " << (success ? "success" : "failure") << endl;

	if (CommonInstanceReference && !CommonInstanceReference->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying CommonInstanceReference " << (success ? "success" : "failure") << endl;

	if (FrameExtraction && !FrameExtraction->verify(list,NULL/*parentlist*/,list/*rootlist*/,verbose,newformat,allpffgitems,log,dict)) success=false;
	if (verbose)
		log << "PhotoacousticImage success after verifying FrameExtraction " << (success ? "success" : "failure") << endl;

	return success;
}


#endif /* __Header_iodcompv__ */
